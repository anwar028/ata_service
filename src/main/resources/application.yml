spring:
  application:
    name: ATA
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/ata_db
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
